---
{{- if .Values.secrets.enabled }}
apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: {{ .Values.app.name }}-secrets
  namespace: {{ .Values.app.name }}
spec:
  itemPath: {{ .Values.secrets.itemPath | quote }}
{{- end }}

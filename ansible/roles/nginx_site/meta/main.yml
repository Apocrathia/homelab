---
galaxy_info:
  role_name: nginx_site
  author: apocrathia
  description: Generic reverse-proxy site — nginx vhost with a TLS cert from the in-repo acme_sh role.
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - all
dependencies:
  - role: acme_sh

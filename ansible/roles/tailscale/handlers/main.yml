---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600
  # Handlers fire in check mode when their task reported changed; the repo
  # file was not written, so there is nothing real to refresh.
  when: not ansible_check_mode

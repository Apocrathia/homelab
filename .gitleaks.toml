# Gitleaks configuration
title = "gitleaks config"

[allowlist]
description = "Allowlist false positives in documentation"
paths = [
  '''\.cursor/rules/python\.mdc''',
]

regexes = [
  '''Passwords: Argon2/bcrypt''',
]

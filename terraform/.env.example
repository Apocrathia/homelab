# OpenTofu provider tokens — real values live in 1Password → this file
# (gitignored). Non-secret org/zone identity lives in provider or deployment HCL.

export CLOUDFLARE_API_TOKEN=
export OKTA_API_TOKEN=

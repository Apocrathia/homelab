{
  "note": "symlink-owned vs script-owned: Cursor discovery is per-file .mdc symlinks into .agents/rules/; Claude reads CLAUDE.md -> AGENTS.md. Script-owned output is append_system at .prime/agent/APPEND_SYSTEM.md. always_on is SoT for Cursor alwaysApply:true (humanizer stays glob-only). rules matches always_on as a set; order in rules is concat order. AGENTS.md Always in force omits general and links context/voice.md instead (intentional).",
  "mirrors": {
    "cursor": {
      "enabled": false,
      "path": ".cursor/rules",
      "reason": "symlink owns discovery"
    },
    "claude": {
      "enabled": false,
      "reason": "no Claude rules mirror; CLAUDE.md -> AGENTS.md"
    },
    "prime-agent": {
      "enabled": false,
      "path": ".prime/agent",
      "reason": "append_system owns .prime/agent/APPEND_SYSTEM.md"
    }
  },
  "append_system": {
    "enabled": true,
    "path": ".prime/agent/APPEND_SYSTEM.md",
    "note": "always_on is SoT for Cursor alwaysApply:true. rules matches always_on as a set; order in rules is concat order. AGENTS.md Always in force may omit general and link context/voice.md instead (intentional).",
    "rules": [
      "ambiguity-goes-back-to-source",
      "clarify-dont-guess",
      "context-management",
      "general",
      "ground-before-asking",
      "ponytail",
      "protected-paths",
      "question-format",
      "response-shape",
      "security",
      "stop-loss",
      "subagents",
      "surgical-edits",
      "worktrees"
    ],
    "always_on": [
      "ambiguity-goes-back-to-source",
      "clarify-dont-guess",
      "context-management",
      "general",
      "ground-before-asking",
      "ponytail",
      "protected-paths",
      "question-format",
      "response-shape",
      "security",
      "stop-loss",
      "subagents",
      "surgical-edits",
      "worktrees"
    ]
  }
}

---
# Operator-managed Flux controllers and root Git sync. The sync uses the
# 1Password-backed pull secret and keeps the root Kustomization orphan-safe.
apiVersion: fluxcd.controlplane.io/v1
kind: FluxInstance
metadata:
  name: flux
  namespace: flux-system
spec:
  distribution:
    # Track current flux2 minor; operator reconciles patch releases.
    version: "2.9.x"
    registry: "ghcr.io/fluxcd"
  sync:
    kind: GitRepository
    url: "ssh://git@gitlab.com/apocrathia/homelab"
    ref: "refs/heads/main"
    path: "./flux/manifests/01-bootstrap"
    # OnePasswordItem flux-operator-secrets (identity + known_hosts).
    # Do not use the bootstrap-created Secret named flux-system.
    pullSecret: "flux-operator-secrets"
    interval: 1m
  kustomize:
    patches:
      # Keep root sync orphan-safe (same contract as pre-cutover soften).
      - target:
          kind: Kustomization
          name: flux-system
        patch: |
          - op: replace
            path: /spec/prune
            value: false
          - op: add
            path: /spec/deletionPolicy
            value: Orphan

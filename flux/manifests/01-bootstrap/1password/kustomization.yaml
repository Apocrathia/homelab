---
# yamllint disable-line rule:line-length
# yaml-language-server: $schema=https://json.schemastore.org/kustomization.json
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: onepassword
resources:
  - namespace.yaml
  - helmrelease.yaml
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: onepassword-crs
  namespace: flux-system
spec:
  dependsOn:
    - name: onepassword
  wait: true
  path: ./flux/manifests/01-bootstrap/1password
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
resources:
  - secret.yaml

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: romm-esde-media-import
  namespace: romm
spec:
  # Thursday 05:00 America/Denver — after esde-media-scraper (Wed 04:00+) has
  # refreshed ES-DE media on the Games share.
  schedule: "0 5 * * 4"
  timeZone: "America/Denver"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      activeDeadlineSeconds: 14400
      template:
        spec:
          serviceAccountName: romm-scan-trigger
          automountServiceAccountToken: true
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1001
            runAsGroup: 1001
            fsGroup: 1001
            seccompProfile:
              type: RuntimeDefault
          containers:
            - name: import-esde-media
              # renovate: datasource=docker depName=ghcr.io/trueforge-org/kubectl
              image: ghcr.io/trueforge-org/kubectl:1.31.1@sha256:d1eaced176f09f027aaeaad3203359026ff106764f8b088baae2aa6b640652b8
              command:
                - /bin/sh
                - /scripts/import-trigger.sh
              env:
                - name: COVER_SOURCE
                  value: "miximages"
              resources:
                requests:
                  cpu: 50m
                  memory: 64Mi
                limits:
                  cpu: 200m
                  memory: 128Mi
              securityContext:
                runAsNonRoot: true
                runAsUser: 1001
                readOnlyRootFilesystem: true
                allowPrivilegeEscalation: false
                capabilities:
                  drop:
                    - ALL
              volumeMounts:
                - name: scripts
                  mountPath: /scripts
                  readOnly: true
                - name: tmp
                  mountPath: /tmp
          volumes:
            - name: scripts
              configMap:
                name: romm-scan-scripts
                defaultMode: 0755
            - name: tmp
              emptyDir: {}

Continue this same **run-loop** tick (`mode=scout`). You are the constant-loop
agent; finish this one lap only.

Stay on scout: finish pending read-only scouts, ranking, and the Discord
`#notifications` post.

Do **not** drift into implement-change, draft-commit, cluster mutate, git
commit/push/merge, or protected-path edits. Do not invent work for an empty
queue. Do not start another find-work pass after empty/all-ineligible.

Return only the final tick result (or one clear blocker).

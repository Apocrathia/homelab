# Continue

Continue the dependency-review workflow. If you already returned the verdict
JSON array, reply with it again exactly. If you are still gathering evidence,
finish now — check at most 1–2 upstream issues per suspicious MR, then emit
the fenced json verdict array for every MR in the fact sheet. No prose outside
the json block.

---
apiVersion: v1
kind: Service
metadata:
  name: alert-agent-invoke
  namespace: agent-tasks
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: alert-agent-invoke
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http

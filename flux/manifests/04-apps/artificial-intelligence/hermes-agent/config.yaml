---
# Seed config for first boot. Runtime changes persist on the Longhorn volume at
# /opt/data (HERMES_HOME). LiteLLM model id must match litellm.yml model_name.
model:
  provider: custom
  default: qwen3.5-medium
  base_url: http://litellm.litellm.svc.cluster.local:4000/v1

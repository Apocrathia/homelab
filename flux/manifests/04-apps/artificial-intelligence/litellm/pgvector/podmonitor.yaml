---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: vector-store-postgres
  namespace: litellm
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: vector-store-postgres
      cnpg.io/podRole: instance
  podMetricsEndpoints:
    - port: metrics
      path: /metrics
      interval: 30s

---
apiVersion: kagent.dev/v1alpha1
kind: MCPServer
metadata:
  name: searxng
  namespace: mcp-searxng
  labels:
    app.kubernetes.io/name: searxng
    app.kubernetes.io/component: mcp-server
spec:
  # agentgateway wraps the image entrypoint over stdio and serves HTTP /mcp + /sse.
  transportType: stdio
  stdioTransport: {}
  deployment:
    # renovate: datasource=docker registryUrl=https://docker.io depName=isokoliuk/mcp-searxng
    image: docker.io/isokoliuk/mcp-searxng:1.15@sha256:a1479fd040f54d977df0d2426924723a4f58742bcf1a037e7e098ff22d0b25dc
    port: 8080
    # Image ENTRYPOINT is replaced by agentgateway; restate the MCP process here.
    cmd: node
    args:
      - dist/cli.js
    env:
      SEARXNG_URL: "http://searxng.searxng.svc.cluster.local:8080"
    resources:
      limits:
        cpu: "200m"
        memory: "256Mi"
      requests:
        cpu: "100m"
        memory: "128Mi"
    podSecurityContext:
      seccompProfile:
        type: RuntimeDefault
    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop:
          - ALL

---
apiVersion: kagent.dev/v1alpha1
kind: MCPServer
metadata:
  # Must differ from ToolHive's openzim-mcp Deployment during dual-run.
  name: openzim
  namespace: mcp-openzim
  labels:
    app.kubernetes.io/name: openzim
    app.kubernetes.io/component: mcp-server
spec:
  # ToolHive stdio + streamable-http proxy maps to kmcp's stdio adapter.
  transportType: stdio
  deployment:
    # renovate: datasource=docker registryUrl=https://public.ecr.aws depName=docker/library/python
    image: public.ecr.aws/docker/library/python:3.14-slim
    port: 8080
    cmd: /bin/sh
    args:
      - -c
      - export HOME=/tmp && pip install --quiet openzim-mcp && exec python -m openzim_mcp /data
    env:
      HOME: /tmp
      OPENZIM_MCP_TOOL_MODE: advanced
      OPENZIM_MCP_INSTANCES_PATH: /tmp
    resources:
      limits:
        cpu: "500m"
        memory: "512Mi"
      requests:
        cpu: "100m"
        memory: "256Mi"
    podSecurityContext:
      seccompProfile:
        type: RuntimeDefault
    securityContext:
      allowPrivilegeEscalation: false
      # pip installs at startup and needs a writable root filesystem.
      readOnlyRootFilesystem: false
      capabilities:
        drop:
          - ALL
    volumeMounts:
      - name: zim
        mountPath: /data
        readOnly: true
      - name: tmp
        mountPath: /tmp
    volumes:
      - name: zim
        persistentVolumeClaim:
          claimName: openzim-zim
      - name: tmp
        emptyDir: {}

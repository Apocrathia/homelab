---
apiVersion: kagent.dev/v1alpha1
kind: MCPServer
metadata:
  name: openzim
  namespace: mcp-openzim
  labels:
    app.kubernetes.io/name: openzim
    app.kubernetes.io/component: mcp-server
spec:
  transportType: stdio
  deployment:
    # renovate: datasource=docker registryUrl=https://public.ecr.aws depName=docker/library/python
    image: public.ecr.aws/docker/library/python:3.14-slim@sha256:83ff1d245a3d57d04152252d3ef9cb361494d0b3395abd65a5ebe91c401c8e83
    port: 8080
    cmd: /bin/sh
    args:
      - -c
      - export HOME=/tmp && pip install --quiet openzim-mcp && exec python -m openzim_mcp /data
    env:
      HOME: /tmp
      OPENZIM_MCP_TOOL_MODE: advanced
      OPENZIM_MCP_INSTANCES_PATH: /tmp
    resources:
      limits:
        cpu: "500m"
        memory: "512Mi"
      requests:
        cpu: "100m"
        memory: "256Mi"
    podSecurityContext:
      seccompProfile:
        type: RuntimeDefault
    securityContext:
      allowPrivilegeEscalation: false
      # pip installs at startup and needs a writable root filesystem.
      readOnlyRootFilesystem: false
      capabilities:
        drop:
          - ALL
    volumeMounts:
      - name: zim
        mountPath: /data
        readOnly: true
      - name: tmp
        mountPath: /tmp
    volumes:
      - name: zim
        persistentVolumeClaim:
          claimName: openzim-zim
      - name: tmp
        emptyDir: {}

---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: archiveteam-warrior
  namespace: archiveteam-warrior
spec:
  interval: 15m
  chart:
    spec:
      chart: ./helm/generic-app
      # renovate: datasource=git-tags depName=https://gitlab.com/Apocrathia/homelab.git
      version: "0.0.46"
      sourceRef:
        kind: GitRepository
        name: homelab
        namespace: flux-system
      interval: 5m
  values:
    app:
      name: archiveteam-warrior
      # renovate: datasource=docker depName=atdr.meo.ws/archiveteam/warrior-dockerfile
      image: atdr.meo.ws/archiveteam/warrior-dockerfile@sha256:0404579c23edf10f722d6efc02d026721936ffd3531e303b8f9c071cc8de4788
      replicas: 3
      podAntiAffinity:
        enabled: true
      container:
        name: warrior
        port: 8001
        env:
          - name: DOWNLOADER
            value: "Apocrathia"
          - name: SELECTED_PROJECT
            value: "auto"
          - name: CONCURRENT_ITEMS
            value: "6"
      resources:
        requests:
          cpu: 100m
          memory: 256Mi
        limits:
          cpu: 500m
          memory: 512Mi
      securityContext:
        readOnlyRootFilesystem: "false"
      service:
        port: 80
        targetPort: 8001

    storage:
      longhorn:
        enabled: false
      smb:
        enabled: false

    secrets:
      enabled: false

    authentik:
      enabled: true
      category: "Media"
      displayName: "ArchiveTeam Warrior"
      externalHost: "https://archiveteam-warrior.gateway.services.apocrathia.com"
      icon: "https://gitlab.com/Apocrathia/homelab/-/raw/main/flux/manifests/04-apps/media/acquisitions/archiveteam-warrior/icon.png"

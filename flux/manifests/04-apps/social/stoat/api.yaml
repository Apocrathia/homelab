---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: stoat-api
  namespace: stoat
spec:
  interval: 15m
  chart:
    spec:
      chart: ./helm/generic-app
      # renovate: datasource=git-tags depName=https://gitlab.com/Apocrathia/homelab.git
      version: "0.0.77"
      sourceRef:
        kind: GitRepository
        name: homelab
        namespace: flux-system
      interval: 5m
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    app:
      name: stoat-api
      namespace: stoat
      # renovate: datasource=docker depName=ghcr.io/stoatchat/api
      image: ghcr.io/stoatchat/api:v0.15.3@sha256:9d9cc9119a61ce1477f08fce05387fbfca7cf4d939dd8b69d33851ad4790c6c5
      container:
        name: api
        port: 14702
        env:
          - name: TZ
            value: "America/Denver"
          - name: REVOLT__DATABASE__MONGODB
            value: "mongodb://stoat-mongo:27017"
          - name: REVOLT__DATABASE__REDIS
            value: "redis://stoat-valkey:6379/"
          - name: REVOLT__RABBIT__HOST
            value: "stoat-rabbitmq"
          - name: REVOLT__RABBIT__USERNAME
            valueFrom:
              secretKeyRef:
                name: stoat-secrets
                key: rabbit-username
          - name: REVOLT__RABBIT__PASSWORD
            valueFrom:
              secretKeyRef:
                name: stoat-secrets
                key: rabbit-password
          - name: REVOLT__FILES__ENCRYPTION_KEY
            valueFrom:
              secretKeyRef:
                name: stoat-secrets
                key: files-encryption-key
          - name: REVOLT__FILES__S3__ENDPOINT
            value: "http://stoat-rustfs:9000"
          - name: REVOLT__FILES__S3__PATH_STYLE_BUCKETS
            value: "true"
          - name: REVOLT__FILES__S3__REGION
            value: "us-east-1"
          - name: REVOLT__FILES__S3__ACCESS_KEY_ID
            valueFrom:
              secretKeyRef:
                name: stoat-secrets
                key: rustfs-access-key
          - name: REVOLT__FILES__S3__SECRET_ACCESS_KEY
            valueFrom:
              secretKeyRef:
                name: stoat-secrets
                key: rustfs-secret-key
          - name: REVOLT__FILES__S3__DEFAULT_BUCKET
            value: "revolt-uploads"
          - name: REVOLT__PUSHD__VAPID__PRIVATE_KEY
            valueFrom:
              secretKeyRef:
                name: stoat-secrets
                key: vapid-private-key
          - name: REVOLT__PUSHD__VAPID__PUBLIC_KEY
            valueFrom:
              secretKeyRef:
                name: stoat-secrets
                key: vapid-public-key
      replicas: 1
      podAntiAffinity:
        enabled: false
      resources:
        requests:
          cpu: 100m
          memory: 256Mi
        limits:
          cpu: 500m
          memory: 256Mi
      securityContext:
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        runAsNonRoot: true
        allowPrivilegeEscalation: false
        # Distroless images do no writes, but autumn/january/gifbox process
        # media and may need scratch space outside a read-only rootfs.
        readOnlyRootFilesystem: false
      service:
        type: ClusterIP
        port: 14702
        targetPort: 14702
        portName: http
      healthChecks:
        livenessProbe:
          enabled: true
          tcpSocket:
            port: 14702
          initialDelaySeconds: 15
          periodSeconds: 20
          failureThreshold: 3
        readinessProbe:
          enabled: true
          tcpSocket:
            port: 14702
          initialDelaySeconds: 5
          periodSeconds: 10
          failureThreshold: 3
      volumes:
        configMap:
          - name: revolt-config
            mountPath: /Revolt.toml
            subPath: Revolt.toml
            configMapName: stoat-config

    storage:
      longhorn:
        enabled: false
      smb:
        enabled: false

    postgres:
      enabled: false

    secrets:
      enabled: false

    authentik:
      enabled: false

    gatus:
      enabled: false

    httproute:
      enabled: false

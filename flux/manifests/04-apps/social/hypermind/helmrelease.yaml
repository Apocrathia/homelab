---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: hypermind
  namespace: hypermind
spec:
  interval: 15m
  chart:
    spec:
      chart: ./helm/generic-app
      # renovate: datasource=git-tags depName=https://gitlab.com/Apocrathia/homelab.git
      version: "0.0.76"
      sourceRef:
        kind: GitRepository
        name: homelab
        namespace: flux-system
      interval: 5m
  values:
    app:
      name: hypermind
      # renovate: datasource=docker depName=ghcr.io/lklynet/hypermind-swarm
      image: ghcr.io/lklynet/hypermind-swarm:1.5.0@sha256:62f5b81b5e007060ec4068ffa11fc28019c5600cd8bd620c0e404faa9658cc6f
      replicas: 1
      container:
        name: hypermind
        port: 3000
        env:
          - name: TZ
            value: "America/Denver"
          - name: PORT
            value: "3000"
          - name: STORAGE_PATH
            value: "/tmp/storage"
          - name: DEVICE_PERSISTENCE
            value: "false"
      volumes:
        emptyDir:
          - name: storage
            mountPath: /tmp/storage
      service:
        port: 80
        targetPort: 3000
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        readOnlyRootFilesystem: false
        runAsNonRoot: true
        allowPrivilegeEscalation: false
      resources:
        requests:
          cpu: 50m
          memory: 64Mi
        limits:
          cpu: 500m
          memory: 256Mi
      healthChecks:
        livenessProbe:
          enabled: true
          httpGet:
            path: /api/stats
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 5
        readinessProbe:
          enabled: true
          httpGet:
            path: /api/stats
            port: 3000
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3

    secrets:
      enabled: false

    authentik:
      enabled: true
      mode: "proxy"
      displayName: "Hypermind"
      externalHost: "https://hypermind.gateway.services.apocrathia.com"
      icon: "https://gitlab.com/Apocrathia/homelab/-/raw/main/flux/manifests/04-apps/social/hypermind/icon.svg"
      category: "Social"

    httproute:
      enabled: false

    loadbalancer:
      enabled: false

---
# yaml-language-server: $schema=https://goauthentik.io/blueprints/schema.json
version: 1
metadata:
  name: mealie
  labels:
    blueprints.goauthentik.io/instantiate: "true"
context: {}
entries:
  # OIDC Provider for Mealie
  - model: authentik_providers_oauth2.oauth2provider
    state: present
    identifiers:
      name: "mealie-oidc-provider"
    attrs:
      name: "mealie-oidc-provider"
      authorization_flow: !Find [authentik_flows.flow, [slug, "default-provider-authorization-implicit-consent"]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, "default-invalidation-flow"]]
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "openid"]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "email"]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "profile"]]
      sub_mode: "user_username"
      include_claims_in_id_token: true
      issuer_mode: "per_provider"
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, "authentik Self-signed Certificate"]]
      redirect_uris:
        - url: "https://mealie.gateway.services.apocrathia.com/login"
          matching_mode: "strict"
        - url: "https://mealie.gateway.services.apocrathia.com/login?direct=1"
          matching_mode: "strict"
      access_code_validity: "minutes=1"
      access_token_validity: "minutes=5"
      refresh_token_validity: "days=30"
      token_validity: "days=30"
      client_type: "confidential"

  # Application for Mealie
  - model: authentik_core.application
    state: present
    identifiers:
      slug: "mealie"
    attrs:
      name: "Mealie"
      slug: "mealie"
      provider: !Find [authentik_providers_oauth2.oauth2provider, [name, "mealie-oidc-provider"]]
      launch_url: "https://mealie.gateway.services.apocrathia.com"
      group: "Home"
      open_in_new_tab: true
      icon: "https://gitlab.com/Apocrathia/homelab/-/raw/main/flux/manifests/04-apps/management/mealie/icon.svg"
      meta_description: "Self-hosted recipe manager and meal planning application"
      meta_publisher: "Mealie"

---
# yaml-language-server: $schema=https://goauthentik.io/blueprints/schema.json
version: 1
metadata:
  name: demo-app
  labels:
    blueprints.goauthentik.io/instantiate: "true"
context: {}
entries:
  # Proxy Provider for Demo App
  - model: authentik_providers_proxy.proxyprovider
    state: present
    identifiers:
      name: "demo-app-proxy-provider"
    attrs:
      name: "demo-app-proxy-provider"
      # prettier-ignore
      authorization_flow: !Find [authentik_flows.flow, [slug, "default-provider-authorization-implicit-consent"]]
      # prettier-ignore
      invalidation_flow: !Find [authentik_flows.flow, [slug, "default-invalidation-flow"]]
      # prettier-ignore
      authentication_flow: !Find [authentik_flows.flow, [slug, "default-authentication-flow"]]
      mode: "proxy"
      intercept_header_auth: false
      internal_host: "http://demo-app.demo-app.svc.cluster.local:80"
      external_host: "https://demo.gateway.services.apocrathia.com"

  # Application for Demo App
  - model: authentik_core.application
    state: present
    identifiers:
      slug: "demo-app"
    attrs:
      name: "Demo Application"
      slug: "demo-app"
      # prettier-ignore
      provider: !Find [authentik_providers_proxy.proxyprovider, [name, "demo-app-proxy-provider"]]
      launch_url: "https://demo.gateway.services.apocrathia.com"
      open_in_new_tab: true
      icon: "https://i.imgur.com/0gNsvyk.png"

  # Outpost for Demo App
  - model: authentik_outposts.outpost
    state: present
    identifiers:
      name: "demo-app-outpost"
    attrs:
      name: "demo-app-outpost"
      type: "proxy"
      # prettier-ignore
      service_connection: !Find [authentik_outposts.kubernetesserviceconnection, [name, "Local Kubernetes Cluster"]]
      providers:
        # prettier-ignore
        - !Find [authentik_providers_proxy.proxyprovider, [name, "demo-app-proxy-provider"]]
      config:
        authentik_host: "https://auth.gateway.services.apocrathia.com"
        authentik_host_insecure: true
        authentik_host_browser: ""
        log_level: "info"
        object_naming_template: "ak-outpost-%(name)s"
        kubernetes_replicas: 1
        kubernetes_namespace: "authentik"
        kubernetes_httproute_parent_refs:
          - name: main-gateway
            namespace: cilium-system
            sectionName: https

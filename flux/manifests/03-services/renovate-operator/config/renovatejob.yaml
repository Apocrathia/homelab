---
apiVersion: renovate-operator.mogenius.com/v1alpha1
kind: RenovateJob
metadata:
  name: homelab
  namespace: renovate-operator
spec:
  schedule: "0 3 * * *"
  discoveryFilters:
    - "Apocrathia/*"
  # renovate: datasource=docker depName=ghcr.io/renovatebot/renovate
  image: ghcr.io/renovatebot/renovate:44.52.1@sha256:6922c734849f2ed1c01e3a7924bd60e25e0ea904731a67bad72b02d563c0c3c3
  secretRef: renovate-operator-secret
  provider:
    name: gitlab
    # endpoint omitted: defaults to https://gitlab.com/api/v4
  # Registry credentials for detectHostRulesFromEnv: Docker Hub access token
  # (rate limits) and GHCR via GitHub PAT (read:packages). Keys live on the
  # renovate-operator-secret OnePasswordItem. Carried over from Mend CE.
  extraEnv:
    - name: DOCKER_DOCKER_IO_USERNAME
      valueFrom:
        secretKeyRef:
          name: renovate-operator-secret
          key: dockerhub-username
    - name: DOCKER_DOCKER_IO_PASSWORD
      valueFrom:
        secretKeyRef:
          name: renovate-operator-secret
          key: dockerhub-token
    - name: DOCKER_GHCR_IO_USERNAME
      valueFrom:
        secretKeyRef:
          name: renovate-operator-secret
          key: github-username
    - name: DOCKER_GHCR_IO_PASSWORD
      valueFrom:
        secretKeyRef:
          name: renovate-operator-secret
          key: github-token
  parallelism: 1
  renovateConfig:
    inline: |
      module.exports = {
        binarySource: "global",
        onboarding: false,
        requireConfig: "optional",
        detectHostRulesFromEnv: true,
      }
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 1000m
      memory: 1Gi

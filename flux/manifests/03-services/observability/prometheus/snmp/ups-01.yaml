---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/monitoring.coreos.com/scrapeconfig_v1alpha1.json
# ScrapeConfig for SNMP devices
apiVersion: monitoring.coreos.com/v1alpha1
kind: ScrapeConfig
metadata:
  name: ups-01
  namespace: prometheus-system
  labels:
    release: kube-prometheus-stack
spec:
  staticConfigs:
    - targets:
        - ups-01.iot.apocrathia.com
      labels:
        job: snmp
        module: apc_ups
        device_type: ups
  scrapeInterval: 60s
  scrapeTimeout: 30s
  metricsPath: /snmp
  params:
    module:
      - apc_ups
    auth:
      - snmpv3
  # Relabeling to route through the SNMP exporter
  relabelings:
    # Store the original target as a param
    - sourceLabels: [__address__]
      targetLabel: __param_target
    # Use the target as the instance label
    - sourceLabels: [__param_target]
      targetLabel: instance
    # Point to the SNMP exporter service
    - targetLabel: __address__
      replacement: snmp-exporter.prometheus-system.svc:9116

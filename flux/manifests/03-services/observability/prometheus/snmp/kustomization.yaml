---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: prometheus-system
resources:
  - targets

configMapGenerator:
  - name: snmp-modules
    files:
      - snmp.yml
    options:
      disableNameSuffixHash: true

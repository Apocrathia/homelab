---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization.json
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: grafana-alerting

resources:
  - notification-policy.yaml
  - discord.yaml
  - slack.yaml
  - pushover.yaml
  - latest-image-tags.yaml
  - pvc-capacity.yaml

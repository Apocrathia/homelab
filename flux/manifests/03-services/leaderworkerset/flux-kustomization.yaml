---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: services-leaderworkerset
  namespace: flux-system
spec:
  # Recreate resources when immutable fields change (e.g. Deployment selectors on LWS upgrades).
  force: true
  interval: 10m0s
  path: ./flux/manifests/03-services/leaderworkerset
  prune: true
  sourceRef:
    kind: GitRepository
    name: homelab
  dependsOn:
    - name: flux-system

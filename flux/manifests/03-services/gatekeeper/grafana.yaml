---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: gatekeeper
  namespace: prometheus-system
spec:
  instanceSelector:
    matchLabels:
      app: grafana
  datasources:
    - datasourceName: Prometheus
      inputName: DS_PROMETHEUS
    - datasourceName: Prometheus
      inputName: DS_AIK8S-PROMETHEUS-1
  grafanaCom:
    id: 21875
  folder: Security
